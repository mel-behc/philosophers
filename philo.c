#include "philo.h"

int main()
{
    // int NbrOfArgs;

    // NbrOfArg = ac - 1;
    // if (ac == 1)
    dwd//     return (0);
    // else
    //     printf("%s\n", av[NbrOfArgs - 1]);
    printf("hello Bader \n");
    return 0;
}
